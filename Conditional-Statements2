'''
Created on Feb 22, 2017

Write a Python function is_even that takes as input the parameter number (an integer) and returns True 
if number is even and False if number is odd. Hint: Apply the remainder operator to n (i.e., number % 2) 
and compare to zero. Even template --- Even solution --- Even (Checker)
'''
print "hello"

# Check if number is even 
def is_even(number):
    return (number %2) == 0    
    
def test (number):
    if is_even(number):
        print' Number', number, 'is an even number'
    else:
        print' Number', number, 'is an odd number'
    
test (10)
test (11)
